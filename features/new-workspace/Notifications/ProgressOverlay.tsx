import React from 'react';

/**
 * ProgressOverlay was deprecated and disabled per user request:
 * The floating card in the bottom-right occupied viewport space redundantly
 * because live execution progress is actively displayed in the Right Panel
 * (LiveExecutionPanel) and the 3D Viewport HUD.
 */
export const ProgressOverlay: React.FC = () => null;
