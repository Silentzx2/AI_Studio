# AI 3D Studio Backend — Python 3.12+
# GPU-ONLY MODE: CUDA-enabled PyTorch is required.
# PyTorch is installed via Dockerfile with CUDA 12.1 support.

# Web framework
fastapi==0.115.6
requests==2.31.0
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Pydantic / Settings
pydantic==2.10.4
pydantic-settings==2.7.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
numpy<2.0
greenlet>=3.1.0

# Redis / Celery
redis==5.2.1
celery==5.4.0
kombu==5.4.2

# HTTP client
httpx==0.28.1
aiohttp==3.9.1
aiofiles==24.1.0

# Image processing
Pillow==11.1.0

# 3D mesh processing
trimesh==4.5.3

# Tensor operations (TripoSR dependency)
einops>=0.7.0

# Model weight downloads
huggingface_hub==0.27.1

# OpenAI (prompt enhancement only)
openai==1.60.1

# System monitoring
psutil==6.1.1
pynvml>=11.5.0

# Diffusers (SDXL image generation)
diffusers>=0.30.0
safetensors==0.4.1
accelerate==0.30.0
transformers==4.44.2
torchvision>=0.16.0

# Dev / testing
pytest==8.3.4
pytest-asyncio==0.24.0

# NOTE: PyTorch is installed separately in the Dockerfile with CUDA support:
# pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# NOTE: xformers is optional — install separately if needed:
# pip install xformers==0.0.24
