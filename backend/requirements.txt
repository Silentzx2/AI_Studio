--extra-index-url https://mirrors.aliyun.com/pypi/simple

# ── Core dependencies (pinned for torch 2.8.0 compatibility) ──────────
# torch 2.8.0+cu128 does NOT have torch.float8_e8m0fnu
# transformers must be <4.44 to avoid finegrained_fp8 import error
# diffusers must match transformers version
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# transformers/diffusers version pinning to avoid torch.float8_e8m0fnu
# and PreTrainedModel circular import issues
transformers==4.43.2
diffusers==0.24.0
huggingface_hub>=0.24.0

# Core FastAPI and web server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP and middleware
httpx==0.25.2
python-multipart==0.0.31

# Configuration and data handling
PyYAML==6.0.1
python-dotenv==1.2.2

# Database
sqlalchemy==2.0.23

# File handling and async I/O
aiofiles==23.2.1
Pillow==12.3.0

# GPU monitoring and system info
GPUtil==1.4.0
psutil==5.9.6

# 3D processing libraries
trimesh==4.6.12
open3d==0.18.0
numpy==1.24.3

# Development and testing
pytest==9.0.3
pytest-asyncio==0.21.1
black==26.3.1
flake8==6.1.0
mypy==1.7.1

# Logging and monitoring
structlog==23.2.0

# Security
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.4.0

# Optional: For production deployment
gunicorn==22.0.0
redis==5.0.1  # For caching and job queues

# tsinghua endpoint not work for tb-nightly 
tb-nightly
pydantic[email]
realesrgan==0.3.0
peft==0.17.1

# extra 
pyvista 
vtk

# Build toolchain
ninja
setuptools
wheel
cython
packaging
setuptools-scm
scikit-build-core
pybind11
